{
  "name": "studio-naturalmente-bela",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^5.5.0",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "npm run prebuild-check && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "optimize-images": "node optimize-images.js",
    "prebuild-check": "node prebuild-check.js",
    "predeploy": "npm run optimize-images",
    "deploy": "npm run build && vercel --prod"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
